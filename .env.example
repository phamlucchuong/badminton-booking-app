# Monorepo note:
# - Backend runtime variables live in backend/.env
# - Web admin runtime variables live in frontend/apps/web-admin/.env
# - This root file is kept only as a legacy fallback for local setups
